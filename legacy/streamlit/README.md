# Legacy Streamlit proof of concept

This UI is archived for implementation history and is not supported as the Aizen product surface. Install it only with `uv sync --group legacy`, then run `uv run --group legacy streamlit run legacy/streamlit/main.py`.
