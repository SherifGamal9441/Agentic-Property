# ADR 005: Frozen snapshot and enrichment fallback

**Status:** Accepted

The showcase uses a DVC-frozen listing snapshot. One controlled enrichment attempt did not prove unit size or dedicated parking, so those features are removed. Building totals remain separately named and cannot substitute for unit facts.
